server {
    listen 80;
    listen [::]:80;
    server_name asterisk-gui.local;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    root /var/www/public;
    index index.php index.html index.htm;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;

    charset utf-8;

    # Logging
    access_log /var/log/nginx/asterisk-gui-access.log;
    error_log /var/log/nginx/asterisk-gui-error.log;

    # Main location
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Deny access to hidden files
    location ~ /\.(?!well-known) {
        deny all;
    }

    # Favicon and robots.txt
    location = /favicon.ico { 
        access_log off; 
        log_not_found off; 
    }
    
    location = /robots.txt { 
        access_log off; 
        log_not_found off; 
    }

    # PHP handling
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_index index.php;
        include fastcgi_params;
        
        # Increase timeouts for long-running requests
        fastcgi_read_timeout 300;
        fastcgi_send_timeout 300;
    }

    # Static assets caching
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Recordings download
    location /recordings {
        alias /var/spool/asterisk/monitor;
        internal;
    }

    # Client max body size (for file uploads)
    client_max_body_size 100M;

    # Buffer size
    client_body_buffer_size 128k;
}

# HTTPS configuration (uncomment and configure after SSL setup)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name asterisk-gui.local;
# 
#     # SSL certificates
#     ssl_certificate /etc/nginx/ssl/asterisk-gui.crt;
#     ssl_certificate_key /etc/nginx/ssl/asterisk-gui.key;
# 
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
# 
#     # HSTS
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
# 
#     # Rest of the configuration same as HTTP
#     root /var/www/public;
#     index index.php index.html index.htm;
#     
#     # ... (copy all location blocks from above)
# }

# WebSocket configuration (if using Laravel Reverb)
# upstream websocket {
#     server 127.0.0.1:8080;
# }
# 
# server {
#     listen 6001;
#     server_name asterisk-gui.local;
# 
#     location / {
#         proxy_pass http://websocket;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "Upgrade";
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }

